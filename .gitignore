.gradle/
.kotlin/
.cxx/
.idea/
local.properties
**/build/
captures/
*.iml
# Signing keys never enter the repo; CI restores them from secrets.
release.keystore
*.jks
*.keystore
# Nested reference clones (upstream + Extended) stay local only.
Root-My-Galaxy/
Root-My-Galaxy-Extended/
# Bundled production payloads (~24MB) are regenerated by
# .github/scripts/prepare_production_payload_bundle.py in CI / locally.
app/src/main/assets/production-payloads/
